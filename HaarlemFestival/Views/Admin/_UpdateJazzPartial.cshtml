@using HaarlemFestival.Models;
@model int

@using (Ajax.BeginForm("UpdateJazz", "Admin", new AjaxOptions { HttpMethod = "POST" }))
{
    Html.RenderAction("_JazzPartial", "Admin", new { id = Model });
}

