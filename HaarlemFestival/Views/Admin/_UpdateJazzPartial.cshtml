@using HaarlemFestival.Models;
@model int

@using (Ajax.BeginForm("UpdateJazz", "Admin", new AjaxOptions { HttpMethod = "POST" }, new { @class = "manageEvent formStyle" }))
{
    Html.RenderAction("_JazzPartial", "Admin", new { id = Model });
}

