@using HaarlemFestival.Models;
@model int

@using (Ajax.BeginForm("UpdateHistoric", "Admin", new AjaxOptions { HttpMethod = "POST" }))
{
    Html.RenderAction("_HistoricPartial", "Admin", new { id = Model });
}
