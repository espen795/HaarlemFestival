@using HaarlemFestival.Models
@model Dinner

<script type="text/javascript">

</script>

@using (Html.BeginForm("AddDinner", "Admin", FormMethod.Post))
{
    <div class="form-group">
        <div class="input-group">
            @Html.Label("Restaurant:")
            @Html.DropDownListFor(d => d.Restaurant, new SelectList((List<Restaurant>)ViewData["Restaurants"], "RestaurantId", "Naam"))
        </div>
    </div>

    <div class="form-group">
        <div class="input-group">
            @Html.Label("Start:")
            <input type="date" id="Date" /> @Html.Label("") <input type="time" id="StartSession" /> - <input type="time" id="EndSession" />
        </div>
    </div>
    <div class="form-group">
        <div class="input-group">
            @Html.Label("Price:")
            @Html.EditorFor(d => d.Price)
        </div>
    </div>
    <div class="form-group">
        <div class="input-group">
            @Html.Label("Price (12-):")
            @Html.EditorFor(d => d.AlternativePrice)
        </div>
    </div>
    <div class="form-group">
        <div class="input-group">
            @Html.Label("Seats: ")
            @Html.EditorFor(d => d.TotalTickets)
        </div>
    </div>
    <div class="form-group">
        <div class="input-group">
            @Html.Label("Phonenumber: ")
            @Html.EditorFor(d => d.Phonenumber)
        </div>
    </div>
    <div class="form-group">
        <div class="input-group">
            @Html.Label("Address: ")
            @Html.EditorFor(d => d.Address)
        </div>
    </div>

    <input type="submit" value="Add Dinner" />
 }
