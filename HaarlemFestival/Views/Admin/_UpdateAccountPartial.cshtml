@using HaarlemFestival.Models;
@model int

@using (Html.BeginForm("UpdateAccount", "Admin", FormMethod.Post, new { @class = "manageEvent formStyle" }))
{
    Html.RenderAction("_AccountPartial", "Admin", new { id = Model });
}