@using HaarlemFestival.Models;
@model int

@using (Ajax.BeginForm("UpdateTalking", "Admin", new AjaxOptions { HttpMethod = "POST" }))
{
    Html.RenderAction("_TalkingPartial", "Admin", new { id = Model });
}
