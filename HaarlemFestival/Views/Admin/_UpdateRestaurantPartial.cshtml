@using HaarlemFestival.Models;
@model int

@using (Ajax.BeginForm("UpdateRestaurant", "Admin", new AjaxOptions { HttpMethod = "POST" }))
{
    Html.RenderAction("_RestaurantPartial", "Admin", new { id = Model });
}

