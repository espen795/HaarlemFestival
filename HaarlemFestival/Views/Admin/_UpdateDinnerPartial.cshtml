@using HaarlemFestival.Models;
@model int

@using (Ajax.BeginForm("UpdateDinner", "Admin", new AjaxOptions { HttpMethod = "POST" }))
{
    Html.RenderAction("_DinnerPartial", "Admin", new { id = Model });
}

